---
phase: 03-frontend-foundation
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/web/src/lib/stores/sidebar.svelte.ts
  - apps/web/src/lib/utils/breadcrumbs.ts
  - apps/web/src/lib/components/layout/Sidebar.svelte
  - apps/web/src/lib/components/layout/Topbar.svelte
  - apps/web/src/lib/components/layout/Breadcrumbs.svelte
  - apps/web/src/lib/components/layout/UserMenu.svelte
  - apps/web/src/routes/(app)/+layout.svelte
  - apps/web/src/routes/(app)/+layout.server.ts
  - apps/web/src/routes/(app)/+page.svelte
  - apps/web/src/routes/(app)/clients/+page.svelte
  - apps/web/src/routes/(app)/engagements/+page.svelte
  - apps/web/src/routes/(app)/tasks/+page.svelte
  - apps/web/src/routes/(app)/team/+page.svelte
  - apps/web/src/routes/(app)/settings/+page.svelte
autonomous: true
requirements: [SHELL-03, SHELL-04, SHELL-05, SHELL-06]

must_haves:
  truths:
    - "Authenticated user sees a sidebar with Dashboard, Clients, Engagements, Tasks, Team, Settings navigation"
    - "Sidebar collapses to 64px icon-only view and expands to 240px with labels"
    - "Sidebar state persists in localStorage across page reloads"
    - "Topbar shows breadcrumbs on left and user menu + notification bell on right"
    - "User menu dropdown shows Profile, Settings, divider, Sign out (red)"
    - "Clicking Sign out logs user out and redirects to /login"
    - "Breadcrumbs update based on current route path"
    - "On mobile (<768px) sidebar auto-collapses; overlay mode on toggle"
    - "Active nav item has blue-50 background and blue-600 text with left border accent"
  artifacts:
    - path: "apps/web/src/lib/components/layout/Sidebar.svelte"
      provides: "Collapsible sidebar with navigation items"
      min_lines: 80
    - path: "apps/web/src/lib/components/layout/Topbar.svelte"
      provides: "Top bar with breadcrumbs, notification bell, user menu"
      min_lines: 40
    - path: "apps/web/src/lib/components/layout/Breadcrumbs.svelte"
      provides: "Route-based breadcrumb trail"
      min_lines: 20
    - path: "apps/web/src/lib/stores/sidebar.svelte.ts"
      provides: "Sidebar collapsed state with localStorage persistence"
      exports: ["initSidebar", "toggleSidebar", "setSidebarCollapsed", "isSidebarCollapsed"]
    - path: "apps/web/src/routes/(app)/+layout.svelte"
      provides: "App shell layout composing Sidebar + Topbar + content area"
      min_lines: 30
  key_links:
    - from: "apps/web/src/lib/components/layout/Sidebar.svelte"
      to: "apps/web/src/lib/stores/sidebar.svelte.ts"
      via: "isSidebarCollapsed/toggleSidebar for state"
      pattern: "isSidebarCollapsed|toggleSidebar"
    - from: "apps/web/src/lib/components/layout/Breadcrumbs.svelte"
      to: "apps/web/src/lib/utils/breadcrumbs.ts"
      via: "label map for route segments"
      pattern: "breadcrumbLabel|ROUTE_LABELS"
    - from: "apps/web/src/lib/components/layout/Topbar.svelte"
      to: "/api/auth/logout"
      via: "Sign out action calls logout then clears auth"
      pattern: "auth/logout|clearAuth"
---

<objective>
Build the authenticated app shell — sidebar, topbar, breadcrumbs, responsive behavior, and placeholder pages for each nav item.

Purpose: This is the container every feature page lives inside. Users need the sidebar for navigation, topbar for context (breadcrumbs, user info), and the shell needs to be responsive. This plan creates the layout infrastructure that Phases 4-6 fill with real pages.

Output: A fully functional app shell that wraps all authenticated routes with sidebar navigation, topbar, breadcrumbs, and placeholder pages for each navigation item.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend-foundation/03-CONTEXT.md
@.planning/phases/03-frontend-foundation/03-UI-SPEC.md
@.planning/phases/03-frontend-foundation/03-RESEARCH.md
@.planning/phases/03-frontend-foundation/03-01-SUMMARY.md

<interfaces>
<!-- From 03-01: Auth store (apps/web/src/lib/stores/auth.svelte.ts) -->
export function getAccessToken(): string | null;
export function setAuth(token: string, user: AuthUser): void;
export function clearAuth(): void;
export function getUser(): AuthUser | null;

interface AuthUser {
  id: string;
  email: string;
  fullName: string;
  role: UserRole;
  firmId: string;
  firmName: string;
  avatarUrl: string | null;
}

<!-- From 03-01: API wrapper (apps/web/src/lib/utils/api.ts) -->
export async function api<T>(path: string, options?: RequestInit): Promise<T>;

<!-- From 03-01: Toast store (apps/web/src/lib/stores/toast.svelte.ts) -->
export function addToast(message: string, variant?: ToastVariant): void;

<!-- From 03-01: Button component -->
Button.svelte: variant, size, loading, disabled, type, onclick, children

<!-- From 03-01: app.d.ts -->
App.Locals: { accessToken: string, user?: { id, email, fullName, role, firmId, firmName, avatarUrl } }
App.PageData: { user?: App.Locals['user'], accessToken?: string }

<!-- Backend endpoints needed -->
POST /api/auth/logout — Headers: Authorization: Bearer {token}, Response: 204

<!-- Shared package enums -->
UserRole: PARTNER | MANAGER | JUNIOR_CA | ARTICLE | ADMIN
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar store, breadcrumb utility, and layout components</name>
  <files>
    apps/web/src/lib/stores/sidebar.svelte.ts
    apps/web/src/lib/utils/breadcrumbs.ts
    apps/web/src/lib/components/layout/Sidebar.svelte
    apps/web/src/lib/components/layout/Topbar.svelte
    apps/web/src/lib/components/layout/Breadcrumbs.svelte
    apps/web/src/lib/components/layout/UserMenu.svelte
  </files>
  <action>
    **1. sidebar.svelte.ts** — Sidebar state store (Svelte 5 runes, .svelte.ts file):
    - STORAGE_KEY = 'sidebar-collapsed'
    - Use object pattern: `const sidebarState = $state({ collapsed: false })`
    - initSidebar(): read from localStorage (guard typeof window), also set up matchMedia listener for `(max-width: 767px)` — auto-collapse on mobile
    - toggleSidebar(): flip collapsed, persist to localStorage
    - setSidebarCollapsed(value: boolean): set collapsed, persist
    - isSidebarCollapsed(): return sidebarState.collapsed
    - Export all four functions

    **2. breadcrumbs.ts** — Route-to-breadcrumb mapping utility:
    - ROUTE_LABELS map: { '/': 'Dashboard', '/clients': 'Clients', '/engagements': 'Engagements', '/tasks': 'Tasks', '/team': 'Team', '/settings': 'Settings' }
    - Export function `generateBreadcrumbs(pathname: string): Array<{ label: string, href: string | null }>`
    - Split pathname by /, build cumulative paths
    - Look up each segment in ROUTE_LABELS. For unknown segments (UUIDs), use capitalized segment or 'Detail'
    - Last segment: href = null (non-clickable per UI-SPEC)
    - Always include 'Dashboard' as first breadcrumb with href '/'

    **3. Sidebar.svelte** — Per UI-SPEC App Shell layout:
    - Uses $props() for any overrides, but primarily reads from sidebar store
    - Navigation items array (define inside component):
      - Dashboard: LayoutDashboard icon, href '/'
      - Clients: Users icon, href '/clients'
      - Engagements: Briefcase icon, href '/engagements'
      - Tasks: CheckSquare icon, href '/tasks'
      - Team: UserCircle icon, href '/team'
      - Settings: Settings icon, href '/settings'
    - Import icons from lucide-svelte
    - Use `$page.url.pathname` from `$app/stores` to determine active item
    - Active item: bg-blue-50 text-blue-600, left 3px border-l-3 border-blue-600 (per UI-SPEC)
    - Hover: bg-gray-100 (per UI-SPEC)
    - Nav items: h-10 (40px), pl-6 (24px left padding), gap-2 (8px) between items
    - Icon size: w-5 h-5 (20px), gap-3 (12px) to label text
    - Expanded state (240px): show icon + label text
    - Collapsed state (64px): icons only, centered. Add tooltip on hover (title attribute or custom tooltip with 200ms delay per UI-SPEC)
    - Header area: h-16 (64px), show "CA Practice OS" text when expanded, just a small icon/logo when collapsed
    - Toggle button: chevron icon at bottom of sidebar (ChevronLeft when expanded, ChevronRight when collapsed) per UI-SPEC
    - White bg, border-r border-gray-200
    - Fixed position, full height (h-screen)
    - Width transition: `transition-all duration-200 ease-in-out` per UI-SPEC sidebar behavior
    - **Mobile (<768px)**: Overlay mode — when toggled open, render as fixed overlay with backdrop (bg-black/50, click to close). Use sidebarState to track mobile open state.
    - Use `<a>` tags for navigation items (SvelteKit handles client-side nav)

    **4. Topbar.svelte** — Per UI-SPEC App Shell layout:
    - Props: user (App.Locals['user']), from page data
    - Full width (minus sidebar), h-16 (64px), white bg, border-b border-gray-200
    - Left side: Breadcrumbs component
    - Right side: notification bell icon (Bell from lucide-svelte, w-5 h-5, text-gray-500, hover text-gray-700) + user avatar/menu area
    - Notification bell: relative positioned, with a small red dot (w-2 h-2 rounded-full bg-red-500) — for now always hidden (no notifications backend yet), but structure the dot element for future
    - On mobile: add hamburger menu button (Menu icon) on the left that triggers sidebar toggle
    - Fixed position at top, z-10

    **5. UserMenu.svelte** — Dropdown from topbar:
    - Shows user avatar (32px circle). Fallback to initials from fullName (first letter of first + last name) in blue-600 bg, white text
    - Dropdown arrow (ChevronDown icon, small)
    - On click: toggle dropdown menu
    - Dropdown: white bg, rounded-md, shadow-lg, py-2, min-w-[200px]
    - Items: "Profile" (User icon), "Settings" (Settings icon), divider (border-t border-gray-100 my-1), "Sign out" (LogOut icon, text-red-600)
    - Each item: px-4 py-2, text-sm, hover bg-gray-100
    - Sign out handler: POST /api/auth/logout via api() wrapper, then clearAuth(), addToast('You have been signed out.', 'info'), goto('/login')
    - Also delete the access_token cookie — since this runs client-side, make a fetch to a SvelteKit endpoint or just let the hooks.server.ts handle it on next navigation
    - Click outside to close: implement a `use:clickOutside` Svelte action (simple addEventListener on document for click, check if target is outside node)
    - Escape key to close

    **6. Breadcrumbs.svelte** — Layout component:
    - Import `page` from `$app/stores`, use $page.url.pathname
    - Call generateBreadcrumbs(pathname)
    - Render segments with `/` separator in text-gray-400
    - Non-last segments: `<a>` with text-gray-500, hover:text-blue-600
    - Last segment: text-gray-900 font-medium, no link (span)
    - text-sm per UI-SPEC
  </action>
  <verify>
    <automated>cd /Users/jarvis/Developer/CA_JIRA && pnpm --filter web build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Sidebar renders with 6 navigation items (Dashboard, Clients, Engagements, Tasks, Team, Settings)
    - Sidebar collapses to 64px (icons only) and expands to 240px (with labels)
    - Active nav item highlighted with blue-50 bg and blue-600 text
    - Sidebar state persists in localStorage
    - Topbar renders breadcrumbs on left, notification bell + user menu on right
    - User menu dropdown shows Profile, Settings, Sign out options
    - Breadcrumbs update based on current route
    - All builds without TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 2: App shell layout, placeholder pages, and responsive behavior</name>
  <files>
    apps/web/src/routes/(app)/+layout.svelte
    apps/web/src/routes/(app)/+layout.server.ts
    apps/web/src/routes/(app)/+page.svelte
    apps/web/src/routes/(app)/clients/+page.svelte
    apps/web/src/routes/(app)/engagements/+page.svelte
    apps/web/src/routes/(app)/tasks/+page.svelte
    apps/web/src/routes/(app)/team/+page.svelte
    apps/web/src/routes/(app)/settings/+page.svelte
  </files>
  <action>
    **1. (app)/+layout.server.ts** — Server load for authenticated layout:
    - Load function: return { user: locals.user, accessToken: locals.accessToken }
    - This is guaranteed to have user data because hooks.server.ts auth guard runs first and redirects if unauthenticated

    **2. (app)/+layout.svelte** — App shell composing Sidebar + Topbar + content:
    - Accept children snippet from $props()
    - Receive page data (user, accessToken) from +layout.server.ts via `$page.data` or via parent data
    - On mount ($effect): call initSidebar() to restore collapsed state from localStorage
    - On mount ($effect): if accessToken exists, hydrate the client-side auth store via setAuth(accessToken, user) — this ensures the api() wrapper has the token for client-side API calls
    - Layout structure:
      - Sidebar component (fixed left)
      - Main area: ml-[240px] when sidebar expanded, ml-16 when collapsed (transition-all duration-200)
      - Topbar component (fixed top within main area)
      - Content area: pt-16 (below topbar), p-6 (24px padding per UI-SPEC), bg-gray-50, min-h-screen
    - Responsive: Use Tailwind responsive prefixes
      - lg (>=1024px): sidebar expanded 240px (unless user toggled it closed)
      - md (768-1023px): sidebar collapsed 64px
      - sm (<768px): sidebar hidden, topbar shows hamburger button, sidebar overlays on toggle

    **3. (app)/+page.svelte** — Dashboard placeholder:
    - svelte:head title: "Dashboard — CA Practice OS"
    - Heading: "Dashboard" in text-xl font-semibold (Heading per UI-SPEC)
    - Subtitle: "Welcome back, {user.fullName}" in text-sm text-gray-500
    - For now, show a simple card (white bg, rounded-lg, shadow-sm, p-6) with placeholder text: "Your dashboard will show overdue tasks, today's tasks, and recent activity."
    - This page is the redirect target after login per CONTEXT.md decision

    **4. Placeholder pages** for Clients, Engagements, Tasks, Team, Settings:
    - Each page: svelte:head title, Heading with page name, simple placeholder card
    - Content: "Coming soon — this page will be built in Phase {N}."
    - Clients: "Manage your client base..." (Phase 4)
    - Engagements: "Track engagement lifecycle..." (Phase 4)
    - Tasks: "Task management with status tracking..." (Phase 5)
    - Team: "Team workload and leave management..." (Phase 6)
    - Settings: "Firm settings and configuration..." (Phase 6)
    - These ensure navigation works end-to-end and breadcrumbs display correctly for all routes

    **Responsive behavior detail:**
    - When viewport < 768px (mobile):
      - Sidebar is hidden by default (not rendered in DOM or off-screen via -translate-x-full)
      - Topbar shows a hamburger (Menu) icon on the left
      - Clicking hamburger: sidebar slides in from left as overlay (translate-x-0), backdrop appears (bg-black/50)
      - Content area: ml-0 (full width)
      - Clicking backdrop or nav item: closes sidebar overlay
    - When viewport 768-1023px (tablet):
      - Sidebar: 64px collapsed by default
      - Toggle button still works to expand
    - When viewport >= 1024px (desktop):
      - Sidebar: 240px expanded by default (respects localStorage preference)
      - Toggle button to collapse/expand
  </action>
  <verify>
    <automated>cd /Users/jarvis/Developer/CA_JIRA && pnpm --filter web build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Authenticated users see sidebar + topbar + content area layout
    - Dashboard page renders at / with welcome message
    - All 6 nav items navigate to their respective placeholder pages
    - Breadcrumbs update correctly for each route (e.g., Dashboard > Clients)
    - Sidebar is 240px expanded on desktop, 64px collapsed on tablet, hidden overlay on mobile
    - Sign out from user menu logs out and redirects to /login
    - App shell layout builds without errors
  </done>
</task>

</tasks>

<verification>
1. `pnpm --filter web build` completes without errors
2. After login, dashboard shows sidebar with 6 nav items and topbar with breadcrumbs
3. Clicking each nav item navigates to the correct placeholder page with updated breadcrumbs
4. Sidebar toggle collapses to icon-only view and re-expands
5. Refreshing the page preserves sidebar collapsed state
6. Resizing browser below 768px hides sidebar, shows hamburger menu
7. Sign out redirects to /login
</verification>

<success_criteria>
- App shell layout renders correctly with sidebar, topbar, breadcrumbs, content area
- All 6 navigation items work and update breadcrumbs
- Sidebar collapse/expand works with smooth transition and localStorage persistence
- Responsive behavior matches UI-SPEC (3 breakpoints: desktop, tablet, mobile)
- User menu Sign out works end-to-end
- All placeholder pages exist for Phases 4-6 to replace
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend-foundation/03-02-SUMMARY.md`
</output>
