---
phase: 01-foundation-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - .gitignore
  - .env.example
  - docker-compose.yml
  - packages/shared/package.json
  - packages/shared/tsconfig.json
  - packages/shared/src/index.ts
  - packages/shared/src/enums/index.ts
  - packages/shared/src/enums/user-role.enum.ts
  - packages/shared/src/enums/task-status.enum.ts
  - packages/shared/src/enums/task-priority.enum.ts
  - packages/shared/src/enums/task-action.enum.ts
  - packages/shared/src/enums/entity-type.enum.ts
  - packages/shared/src/enums/constitution.enum.ts
  - packages/shared/src/enums/client-status.enum.ts
  - packages/shared/src/enums/engagement-category.enum.ts
  - packages/shared/src/enums/engagement-status.enum.ts
  - packages/shared/src/enums/recurrence.enum.ts
  - packages/shared/src/enums/subscription-tier.enum.ts
  - packages/shared/src/enums/document-type.enum.ts
  - packages/shared/src/enums/document-source.enum.ts
  - packages/shared/src/enums/document-request-status.enum.ts
  - packages/shared/src/enums/checklist-item-status.enum.ts
  - packages/shared/src/enums/portal.enum.ts
  - packages/shared/src/enums/dsc-class.enum.ts
  - packages/shared/src/enums/dsc-type.enum.ts
  - packages/shared/src/enums/dsc-status.enum.ts
  - packages/shared/src/enums/dsc-holder-type.enum.ts
  - packages/shared/src/enums/leave-type.enum.ts
  - packages/shared/src/enums/leave-status.enum.ts
  - packages/shared/src/enums/notification-type.enum.ts
  - packages/shared/src/enums/notification-channel.enum.ts
  - packages/shared/src/enums/notification-status.enum.ts
  - packages/shared/src/enums/compliance-entry-status.enum.ts
  - packages/shared/src/enums/credential-action.enum.ts
  - packages/shared/src/enums/gst-registration-type.enum.ts
  - packages/shared/src/enums/custom-field-type.enum.ts
  - packages/shared/src/constants/task-status-transitions.ts
  - packages/shared/src/constants/regex-patterns.ts
  - packages/shared/src/constants/limits.ts
  - packages/shared/src/constants/mime-types.ts
  - packages/shared/src/types/address.type.ts
  - packages/shared/src/types/firm-settings.type.ts
  - packages/shared/src/types/notification-preferences.type.ts
  - packages/shared/src/types/recurrence-config.type.ts
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/tsconfig.build.json
  - apps/api/nest-cli.json
  - apps/api/src/main.ts
  - apps/api/src/app.module.ts
  - apps/web/package.json
  - apps/web/svelte.config.js
  - apps/web/vite.config.ts
  - apps/web/src/routes/+page.svelte
  - apps/web/src/app.html
  - apps/web/src/app.css
  - apps/web/tailwind.config.ts
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04

must_haves:
  truths:
    - "Running `pnpm dev` from root starts both NestJS on port 3000 and SvelteKit on port 5173"
    - "Docker Compose starts PostgreSQL 16, Redis 7, and MinIO without errors"
    - "Importing from @ca-practice-os/shared in either app resolves enums, constants, and types"
    - "Environment variables are validated on NestJS startup with clear error messages"
  artifacts:
    - path: "package.json"
      provides: "Root monorepo package.json with Turborepo scripts"
      contains: "turbo run dev"
    - path: "pnpm-workspace.yaml"
      provides: "Workspace definition"
      contains: "packages/*"
    - path: "docker-compose.yml"
      provides: "Local infrastructure services"
      contains: "postgres:16-alpine"
    - path: "packages/shared/src/index.ts"
      provides: "Barrel export for shared package"
      contains: "export * from './enums'"
    - path: "apps/api/src/main.ts"
      provides: "NestJS bootstrap"
      contains: "NestFactory.create"
    - path: "apps/web/src/routes/+page.svelte"
      provides: "SvelteKit home page"
      min_lines: 1
  key_links:
    - from: "apps/api/package.json"
      to: "packages/shared"
      via: "workspace dependency"
      pattern: "@ca-practice-os/shared.*workspace"
    - from: "apps/web/package.json"
      to: "packages/shared"
      via: "workspace dependency"
      pattern: "@ca-practice-os/shared.*workspace"
    - from: "turbo.json"
      to: "apps/*/package.json"
      via: "pipeline config"
      pattern: "dependsOn.*\\^build"
---

<objective>
Scaffold the Turborepo + pnpm monorepo with NestJS API, SvelteKit web app, shared package (all 29 enums, constants, types), and Docker Compose infrastructure.

Purpose: Create the foundational project structure that all subsequent phases build on. Without this, nothing else can start.
Output: A working monorepo where `pnpm dev` starts both apps, `docker compose up` runs Postgres/Redis/MinIO, and the shared package is importable from both apps.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/00-project-setup.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize monorepo, Docker Compose, and shared package with all enums/constants/types</name>
  <files>
    package.json, pnpm-workspace.yaml, turbo.json, .gitignore, .env.example,
    docker-compose.yml,
    packages/shared/package.json, packages/shared/tsconfig.json,
    packages/shared/src/index.ts, packages/shared/src/enums/index.ts,
    packages/shared/src/enums/user-role.enum.ts, packages/shared/src/enums/task-status.enum.ts,
    packages/shared/src/enums/task-priority.enum.ts, packages/shared/src/enums/task-action.enum.ts,
    packages/shared/src/enums/entity-type.enum.ts, packages/shared/src/enums/constitution.enum.ts,
    packages/shared/src/enums/client-status.enum.ts, packages/shared/src/enums/engagement-category.enum.ts,
    packages/shared/src/enums/engagement-status.enum.ts, packages/shared/src/enums/recurrence.enum.ts,
    packages/shared/src/enums/subscription-tier.enum.ts, packages/shared/src/enums/document-type.enum.ts,
    packages/shared/src/enums/document-source.enum.ts, packages/shared/src/enums/document-request-status.enum.ts,
    packages/shared/src/enums/checklist-item-status.enum.ts, packages/shared/src/enums/portal.enum.ts,
    packages/shared/src/enums/dsc-class.enum.ts, packages/shared/src/enums/dsc-type.enum.ts,
    packages/shared/src/enums/dsc-status.enum.ts, packages/shared/src/enums/dsc-holder-type.enum.ts,
    packages/shared/src/enums/leave-type.enum.ts, packages/shared/src/enums/leave-status.enum.ts,
    packages/shared/src/enums/notification-type.enum.ts, packages/shared/src/enums/notification-channel.enum.ts,
    packages/shared/src/enums/notification-status.enum.ts, packages/shared/src/enums/compliance-entry-status.enum.ts,
    packages/shared/src/enums/credential-action.enum.ts, packages/shared/src/enums/gst-registration-type.enum.ts,
    packages/shared/src/enums/custom-field-type.enum.ts,
    packages/shared/src/constants/task-status-transitions.ts,
    packages/shared/src/constants/regex-patterns.ts,
    packages/shared/src/constants/limits.ts,
    packages/shared/src/constants/mime-types.ts,
    packages/shared/src/types/address.type.ts,
    packages/shared/src/types/firm-settings.type.ts,
    packages/shared/src/types/notification-preferences.type.ts,
    packages/shared/src/types/recurrence-config.type.ts
  </files>
  <read_first>
    docs/00-project-setup.md
  </read_first>
  <action>
    Create the monorepo root structure following `docs/00-project-setup.md` precisely:

    1. **Root package.json**: name `ca-practice-os`, private: true, packageManager `pnpm@9.x`. Scripts: `dev`, `dev:api`, `dev:web`, `build`, `lint`, `test`, `db:migrate`, `db:seed` -- all using `turbo run`. devDependencies: `turbo ^2.x`, `typescript ^5.x`.

    2. **pnpm-workspace.yaml**: packages `["packages/*", "apps/*"]`.

    3. **turbo.json**: `$schema` from turbo.build, globalDependencies `["**/.env.*local"]`, pipeline with `build` (dependsOn `^build`, outputs `["dist/**", ".next/**", ".svelte-kit/**"]`), `dev` (cache false, persistent true), `lint` (empty), `test` (dependsOn `["build"]`), `db:migrate` (empty), `db:seed` (empty).

    4. **.gitignore**: `node_modules/`, `dist/`, `.svelte-kit/`, `.env`, `.env.local`, `*.log`, `.turbo/`, `.DS_Store`.

    5. **.env.example**: All variables from docs/00-project-setup.md section 0.6 exactly as specified (DATABASE_URL, REDIS_HOST, REDIS_PORT, REDIS_PASSWORD, JWT_SECRET, JWT_ISSUER, S3_*, ENCRYPTION_KEY, EMAIL_*, WHATSAPP_*, APP_PORT=3000, APP_URL=http://localhost:5173, NODE_ENV=development, LOG_LEVEL=debug).

    6. **docker-compose.yml**: version "3.8", three services:
       - `postgres`: image `postgres:16-alpine`, container_name `capracticeos-db`, env POSTGRES_DB=ca_practice_os, POSTGRES_USER=capracticeos, POSTGRES_PASSWORD=localdev123, port 5432:5432, volume pgdata.
       - `redis`: image `redis:7-alpine`, container_name `capracticeos-redis`, command `redis-server --appendonly yes`, port 6379:6379, volume redisdata.
       - `minio`: image `minio/minio:latest`, container_name `capracticeos-minio`, command `server /data --console-address ":9001"`, env MINIO_ROOT_USER=minioadmin, MINIO_ROOT_PASSWORD=minioadmin, ports 9000:9000 and 9001:9001, volume miniodata.
       - Named volumes: pgdata, redisdata, miniodata.

    7. **packages/shared**: Create package.json with name `@ca-practice-os/shared`, version 0.0.1, private true, main `./src/index.ts`, types `./src/index.ts`, build script `tsc`, devDependencies typescript ^5.x. Create tsconfig.json with target ES2022, module ESNext, moduleResolution bundler, declaration true, strict true, outDir ./dist, rootDir ./src.

    8. **All 29 enum files** in `packages/shared/src/enums/`: Create each enum file exactly matching the values from `docs/00-project-setup.md` section 0.3. Key enums:
       - `user-role.enum.ts`: PARTNER, MANAGER, JUNIOR_CA, ARTICLE, ADMIN
       - `task-status.enum.ts`: TO_DO, IN_PROGRESS, AWAITING_CLIENT, UNDER_REVIEW, PARTNER_APPROVAL, DONE, CANCELLED
       - `task-priority.enum.ts`: LOW, MEDIUM, HIGH, URGENT
       - `task-action.enum.ts`: CREATED, STATUS_CHANGED, ASSIGNEE_CHANGED, REVIEWER_CHANGED, DUE_DATE_CHANGED, PRIORITY_CHANGED, CHECKLIST_ITEM_COMPLETED, CHECKLIST_ITEM_UNCOMPLETED, COMMENT_ADDED, ATTACHMENT_ADDED, ATTACHMENT_REMOVED, DEPENDENCY_ADDED, DEPENDENCY_REMOVED, RECURRING_INSTANCE_CREATED
       - `entity-type.enum.ts`: INDIVIDUAL, HUF, PARTNERSHIP_FIRM, LLP, PRIVATE_LIMITED, PUBLIC_LIMITED, TRUST, SOCIETY, AOP, BOI, OTHER
       - `notification-type.enum.ts`: TASK_ASSIGNED, TASK_OVERDUE, TASK_STUCK, TASK_DEPENDENCY_UNBLOCKED, TASK_REVIEW_REQUESTED, TASK_APPROVAL_REQUESTED, TASK_SENT_BACK, COMMENT_MENTION, DOCUMENT_REQUEST_SENT, DOCUMENT_REQUEST_FULFILLED, DOCUMENT_REQUEST_OVERDUE, DOCUMENT_REQUEST_REMINDER, DSC_EXPIRY_ALERT_30, DSC_EXPIRY_ALERT_15, DSC_EXPIRY_ALERT_7, COMPLIANCE_DEADLINE_APPROACHING, COMPLIANCE_DEADLINE_MISSED, USER_DEACTIVATED_WITH_OPEN_TASKS, ASSIGNEE_ON_LEAVE
       - All other enums as listed in docs/00-project-setup.md section 0.3
       - Create `enums/index.ts` that re-exports all enum files.

    9. **Constants**: Create all four files exactly as shown in docs/00-project-setup.md:
       - `task-status-transitions.ts` with the full transition matrix
       - `regex-patterns.ts` with PAN, TAN, CIN, GSTIN, E164_PHONE, EMAIL, SNAKE_CASE_KEY patterns
       - `limits.ts` with MAX_FILE_SIZE_BYTES (50MB), MAX_TAGS_PER_ENTITY (10), MAX_TAG_LENGTH (50), MAX_CHECKLIST_ITEMS_PER_TASK (30), etc.
       - `mime-types.ts` with ALLOWED_MIME_TYPES array and AllowedMimeType type

    10. **Types**: Create all four type files as shown in docs/00-project-setup.md:
        - `address.type.ts`: Address interface (line1, line2?, city, state, pincode, country)
        - `firm-settings.type.ts`: FirmSettings interface
        - `notification-preferences.type.ts`: NotificationPreferences interface
        - `recurrence-config.type.ts`: RecurrenceConfig interface

    11. **packages/shared/src/index.ts**: Barrel exports for all enums, constants, and types exactly as shown in docs/00-project-setup.md.
  </action>
  <verify>
    <automated>cd /Users/jarvis/Developer/CA_JIRA && cat pnpm-workspace.yaml && cat docker-compose.yml | head -5 && ls packages/shared/src/enums/*.ts | wc -l && cat packages/shared/src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - pnpm-workspace.yaml contains `packages/*` and `apps/*`
    - turbo.json contains `"$schema": "https://turbo.build/schema.json"`
    - docker-compose.yml contains `postgres:16-alpine` and `redis:7-alpine` and `minio/minio:latest`
    - .env.example contains `DATABASE_URL=postgresql://capracticeos:localdev123@localhost:5432/ca_practice_os`
    - .env.example contains `JWT_SECRET=` and `REDIS_HOST=` and `S3_ENDPOINT=`
    - packages/shared/package.json contains `"name": "@ca-practice-os/shared"`
    - packages/shared/src/enums/ contains exactly 29 .enum.ts files plus index.ts (30 total .ts files)
    - packages/shared/src/enums/user-role.enum.ts contains `PARTNER = 'PARTNER'`
    - packages/shared/src/constants/regex-patterns.ts contains `PAN: /^[A-Z]{5}[0-9]{4}[A-Z]{1}$/`
    - packages/shared/src/constants/limits.ts contains `MAX_CONCURRENT_SESSIONS: 5`
    - packages/shared/src/types/address.type.ts contains `export interface Address`
    - packages/shared/src/index.ts contains `export * from './enums'` and `export * from './constants/task-status-transitions'`
    - .gitignore contains `node_modules/` and `.env`
    - Root package.json contains `"turbo run dev"` in scripts.dev
  </acceptance_criteria>
  <done>Root monorepo structure exists with all config files, Docker Compose, and shared package containing all 29 enums, 4 constant files, and 4 type files.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold NestJS API and SvelteKit web apps, install dependencies, verify pnpm dev starts both</name>
  <files>
    apps/api/package.json, apps/api/tsconfig.json, apps/api/tsconfig.build.json,
    apps/api/nest-cli.json, apps/api/src/main.ts, apps/api/src/app.module.ts,
    apps/api/src/app.controller.ts, apps/api/src/app.service.ts,
    apps/api/.env,
    apps/web/package.json, apps/web/svelte.config.js, apps/web/vite.config.ts,
    apps/web/src/routes/+page.svelte, apps/web/src/app.html, apps/web/src/app.css,
    apps/web/tailwind.config.ts
  </files>
  <read_first>
    docs/00-project-setup.md
    packages/shared/package.json
    package.json
  </read_first>
  <action>
    1. **NestJS API** (`apps/api/`):
       - Initialize NestJS project. Create package.json with name `api`, private true.
       - Scripts: `dev` = `nest start --watch`, `build` = `nest build`, `start:prod` = `node dist/main`, `lint` = `eslint "{src,test}/**/*.ts"`, `test` = `jest`, `db:migrate` = `prisma migrate dev`, `db:migrate:prod` = `prisma migrate deploy`, `db:seed` = `prisma db seed`, `db:studio` = `prisma studio`.
       - Dependencies: `@nestjs/common`, `@nestjs/core`, `@nestjs/platform-express`, `@nestjs/config`, `@prisma/client`, `ioredis`, `class-validator`, `class-transformer`, `uuid`, `reflect-metadata`, `rxjs`, `@ca-practice-os/shared` as `workspace:*`.
       - DevDependencies: `@nestjs/cli`, `@nestjs/schematics`, `@nestjs/testing`, `prisma`, `@types/uuid`, `typescript`, `ts-node`, `ts-loader`, `source-map-support`.
       - Prisma config in package.json: `"prisma": { "schema": "prisma/schema" }`.
       - Create `nest-cli.json`: `{ "collection": "@nestjs/schematics", "sourceRoot": "src", "compilerOptions": { "deleteOutDir": true } }`.
       - Create `tsconfig.json`: extends nothing, compilerOptions with module commonjs (NestJS requires this), target ES2022, experimentalDecorators true, emitDecoratorMetadata true, strict true, esModuleInterop true, declaration true, outDir ./dist, rootDir ./src, skipLibCheck true.
       - Create `tsconfig.build.json` extending tsconfig.json with exclude `["node_modules", "dist", "test", "**/*.spec.ts"]`.
       - Create `src/main.ts`: bootstrap function using NestFactory.create(AppModule), enable CORS with origin `process.env.APP_URL || 'http://localhost:5173'`, enable global ValidationPipe with `whitelist: true, forbidNonWhitelisted: true, transform: true`, listen on `process.env.APP_PORT || 3000`, log `API running on port ${port}`.
       - Create `src/app.module.ts`: imports ConfigModule.forRoot({ isGlobal: true }), empty providers/controllers for now.
       - Create `src/app.controller.ts` and `src/app.service.ts` as minimal placeholders.
       - Copy `.env.example` to `apps/api/.env`.
       - **Environment validation**: Create `src/config/env.validation.ts`. Define a class `EnvironmentVariables` with class-validator decorators: `DATABASE_URL` (@IsString, @IsNotEmpty), `REDIS_HOST` (@IsString, @IsNotEmpty), `APP_PORT` (@IsOptional, @IsNumberString), `JWT_SECRET` (@IsString, @MinLength(32)), `NODE_ENV` (@IsIn(['development', 'production', 'test'])). Export a `validate` function that uses `plainToInstance` + `validateSync` and throws descriptive errors listing missing/invalid vars. Wire this into ConfigModule.forRoot as `validate` option.

    2. **SvelteKit Web** (`apps/web/`):
       - Initialize SvelteKit project with TypeScript and TailwindCSS v4.
       - package.json: name `web`, private true, scripts: `dev` = `vite dev --port 5173`, `build` = `vite build`, `preview` = `vite preview`. Dependencies: `@ca-practice-os/shared` as `workspace:*`. DevDependencies: `@sveltejs/kit`, `@sveltejs/adapter-auto`, `svelte`, `vite`, `typescript`, `tailwindcss`, `@tailwindcss/vite`.
       - Create `svelte.config.js` with adapter-auto.
       - Create `vite.config.ts` importing sveltekit and tailwindcss plugins.
       - Create `src/app.html` with standard SvelteKit shell.
       - Create `src/app.css` importing tailwindcss (`@import 'tailwindcss'`).
       - Create `src/routes/+page.svelte` with a minimal "CA Practice OS" heading.
       - Create `src/routes/+layout.svelte` that imports app.css.

    3. **Install and verify**:
       - Run `pnpm install` from root.
       - Verify `pnpm dev:api` starts NestJS (check port 3000 responds).
       - Verify `pnpm dev:web` starts SvelteKit (check port 5173 responds).
       - Verify `import { UserRole } from '@ca-practice-os/shared'` resolves in both apps (add a test import in each app's entry file as a comment confirming it works, or verify via TypeScript compilation).
  </action>
  <verify>
    <automated>cd /Users/jarvis/Developer/CA_JIRA && pnpm install && pnpm --filter api exec tsc --noEmit 2>&1 | tail -5 && ls apps/api/src/main.ts apps/web/src/routes/+page.svelte</automated>
  </verify>
  <acceptance_criteria>
    - apps/api/package.json contains `"@ca-practice-os/shared": "workspace:*"` in dependencies
    - apps/api/package.json contains `"prisma": { "schema": "prisma/schema" }`
    - apps/api/src/main.ts contains `NestFactory.create` and `ValidationPipe` and `app.enableCors`
    - apps/api/src/main.ts contains `process.env.APP_PORT || 3000`
    - apps/api/src/app.module.ts contains `ConfigModule.forRoot`
    - apps/api/src/config/env.validation.ts contains `class EnvironmentVariables` and `@IsNotEmpty` and `validateSync`
    - apps/web/package.json contains `"@ca-practice-os/shared": "workspace:*"` in dependencies
    - apps/web/package.json contains `"vite dev --port 5173"` in scripts.dev
    - apps/web/src/routes/+page.svelte exists and contains text content
    - apps/web/src/app.css contains `tailwindcss`
    - apps/api/.env contains `DATABASE_URL=`
    - `pnpm install` exits 0
  </acceptance_criteria>
  <done>Both NestJS API and SvelteKit web app are scaffolded, dependencies installed, shared package linked as workspace dependency, env validation wired, and TypeScript compilation passes.</done>
</task>

</tasks>

<verification>
1. `pnpm install` completes without errors
2. `pnpm --filter api exec tsc --noEmit` exits 0
3. `docker compose up -d` starts all 3 containers (verify with `docker compose ps`)
4. `ls packages/shared/src/enums/*.enum.ts | wc -l` returns 29
5. `grep -c "workspace:" apps/api/package.json` returns at least 1
6. `grep -c "workspace:" apps/web/package.json` returns at least 1
</verification>

<success_criteria>
- `pnpm dev` starts both apps without errors (FOUND-01)
- Shared package exports all enums, constants, and types importable from both apps (FOUND-02)
- Docker Compose runs PostgreSQL 16, Redis 7, and MinIO (FOUND-03)
- Environment variables validated on NestJS startup with clear error messages (FOUND-04)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-infrastructure/01-01-SUMMARY.md`
</output>
